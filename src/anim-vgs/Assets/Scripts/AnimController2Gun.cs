using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AnimController2Gun : MonoBehaviour
{
    //[SerializeField] private AimConstraint chestAim;
    [SerializeField] private Transform animRig;
    //public void Aim(InputAction.CallbackContext ctx){
    //    bool val = ctx.readValuesButton();
        //Activar o desactivar el constrain de apundo del pecho
    //    chestAnim.enabled = ctx;

        //Desencadenar la activacion de la camada de apuntado
        //animRig.gameObject.SetActive(val);
    //}
}
